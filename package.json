{
  "name": "mc-afk-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'no build - static site' && cp index.html dist/index.html",
    "start": "serve dist"
  },
  "devDependencies": {
    "serve": "^14.0.1"
  }
}
